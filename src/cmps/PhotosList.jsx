
// export function PhotosList({ photos, removePhoto }) {
//     return (
//         <section className="photo-list simple-cards-grid">
//             {photos.map(photo =>
//                 // <photoPreview removePhoto={removePhoto}  photo={photo} key={photo._id} />
//             )}
//         </section>
//     )
// }
